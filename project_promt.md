# 角色与目标
请你扮演一名精通Python的量化金融工程师。你的任务是，根据我设定的长线价值投资逻辑，编写一套完整的量化分析代码。

# 数据要求
- 数据源：使用 **AkShare** 库获取A股数据（请自动处理网络请求和异常）。
- 目标股票：参数化输入，默认先以股票代码 `000001`（平安银行）为例。
- 数据范围：获取过去 **10年**（2016-01-01 至今）的财务数据（季报/年报）和日频交易数据。

# 任务步骤（核心逻辑）
请严格按照以下逻辑编写代码：

## 第一步：基本面筛选（质量评估）
- 计算并输出该股票过去5年（2021-2025）每年的核心财务指标：
  - 净资产收益率（ROE）
  - 股息率（TTM）
  - 资产负债率
  - 经营性现金流净额 / 净利润（判断利润质量）
- 判断标准：如果连续5年ROE > 15% 且 股息率 > 2%，标记为“初步通过筛选”，否则标记为“不满足”。

## 第二步：估值锚定（内在价值计算）
- 使用 **自由现金流折现模型（DCF）** 估算该公司的内在价值。
- 具体假设：
  - 基于过去5年的平均自由现金流作为基期。
  - 设定未来5年增长率为 10%，永续增长率为 3%。
  - 折现率（WACC）设定为 8%。
- 计算并输出：**每股内在价值估值区间**（请提供保守、中性、乐观三种情景的结果）。

## 第三步：市场情绪辅助（股债性价比）
- 获取当前全市场市盈率的中位数和十年期国债收益率。
- 计算当前的“股债性价比”（市盈率倒数 - 国债收益率）。
- 对比该指标过去5年的历史分位数（0%-100%），并输出当前处于什么位置（低估/合理/高估）。

## 第四步：综合投资建议
- 结合第二步的估值和第三步的市场情绪，给出具体的操作建议：
  - 如果当前股价 < 保守估值，建议“大幅买入”。
  - 如果当前股价介于保守和中性估值之间，建议“分批建仓”。
  - 如果当前股价 > 乐观估值，建议“持有或减仓”。

# 代码输出格式要求
1. 请生成一个完整的 `.py` Python脚本。
2. 脚本中必须包含 `def main()` 主函数。
3. 关键结果请使用 `print()` 清晰打印出来，并尽量使用 `pandas` 的 `DataFrame` 展示数据表格。
4. 请用 `matplotlib` 或 `plotly` 绘制一张**估值走势图**（股价 vs 估算内在价值线的对比图）。
5. 请在所有代码前详细注释每一步的逻辑。

# 运行环境
- Python 3.9+
- 依赖库：akshare, pandas, numpy, matplotlib, plotly, scipy

请现在开始编写代码。